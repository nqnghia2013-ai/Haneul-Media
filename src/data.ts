import { Member, ContentItem, BroadcastInfo } from './types';

export const mockMembers: Member[] = [];
export const mockContents: ContentItem[] = [];
export const mockSchedules: BroadcastInfo[] = [];

